<%= render 'shared/post_error_messages' %>

<%= form_for @post do |f| %>

    <%= f.label :header, "Header:" %>
    <%= f.text_field :header %>

    <%= f.label :content, "Content:" %>
    <%= f.text_area :content %>

    <%= f.submit "Post" %>

<% end %>